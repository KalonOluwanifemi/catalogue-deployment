# Demo environment root module.
#
# STUB - module call to be added.
#
# First apply is deliberately bare: Standard SWA, no app_settings, no custom
# domain, no config file. The only purpose is to generate the
# *.azurestaticapps.net hostname, which the portal app registration needs for
# its redirect URI.
